import React from 'react';

import {
  HeroContainer,
  HeroContent,
  HeroItems,
} from './styles';

const Eventos = () => {
  

  return (
    <HeroContainer>
     
      <HeroContent>
        <HeroItems>         
                  
        </HeroItems>
      </HeroContent>
    </HeroContainer>
  );
};

export default Eventos;